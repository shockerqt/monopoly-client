import './Reconnecting.scss';

const Reconnecting = () => (
  <div className="reconnecting-screen">
    <div className="lds-heart"><div></div></div>
    <div className="reconnecting-text">Reconnecting</div>
  </div>
);

export default Reconnecting;
